<h1 class="text-center">Add a review for <%= @console.name %></h1>

<div class="container">
  <div class="row">
    <div class="col-xs-6 col-xs-offset-3">
      <%= simple_form_for [@console, @review] do |f| %>
        <%= f.input :description %>
        <%= f.input :rating %>
        <%= f.submit "Add a review", class: "btn btn-success" %>
      <% end %>
    </div>
  </div>
</div>
